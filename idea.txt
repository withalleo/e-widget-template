# ─────────────────────────────────────────────────────────────────────────────
#  Describe the Alleo E-widget you want here, in plain language.
#  An AI agent will read this file and generate the widget into the dist/ folder.
#
#  Replace everything below with your own idea. The more detail, the better:
#    - What does the widget do? Who uses it and why?
#    - What should it look like (colors, layout, size)?
#    - What happens when the user interacts with it?
#    - Should its state stay in sync across everyone viewing the board?
#    - Does it need data from an external API? If so, which one?
#    - Should it read or write the content of other objects on the board?
#
#  Lines starting with "#" are notes to you and can be deleted.
# ─────────────────────────────────────────────────────────────────────────────

# Example idea (delete this and write your own):
#
# A collaborative voting widget. Show a question at the top and a list of options
# as large buttons. When anyone clicks an option, increment its vote count and
# keep the totals in sync for everyone viewing the board. Show each option's
# percentage as a horizontal bar. Include a "Reset votes" button. Fire an action
# whenever someone votes so the board can react. Dark theme, blue accent.
